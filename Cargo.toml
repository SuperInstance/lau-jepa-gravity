[package]
name = "lau-jepa-gravity"
version = "0.1.1"
edition = "2021"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-jepa-gravity"
description = "JEPA gravity system — room-based gravity values that algorithmically adjust model parameters"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
